language: python
python:
  - "3.5"
  - "3.6"
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - pylint ipeadata/ipeadata.py
  - python -m unittest ipeadata/tests/test_api.py