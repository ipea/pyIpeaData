language: python
python:
  - "3.5"
  - "3.6"
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - pylint ipeadata/api.py
  - coverage run --source=. -m unittest discover -s test